
import os
from cluster.app import create_app

"""
def test_config():
    # TODO: need to make a fixture factory (for apps with different configs) for this test 
    #testing = create_app().config["TESTING"]
    #print(testing)
    #assert not create_app().config["TESTING"]
    #assert create_app({'TESTING': True, "SQLALCHEMY_DATABASE_URI": "sqlite:///"}).testing
    assert True
"""

