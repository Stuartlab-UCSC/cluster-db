# !/usr/bin/env bash
# start

source $CLUSTERDB/env/bin/activate
export FLASK_APP=cluster
export FLASK_ENV=development

if [ $HTTPS ]; then
    nohup flask run -h $HOST -p $PORT --with-threads --cert $CERT --key $KEY &> log &
else
    flask run -h $HOST -p $PORT --with-threads $USE_HTTPS
fi
